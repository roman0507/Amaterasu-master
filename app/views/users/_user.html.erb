<li>
  <%= image_tag(user.avatar.url, :class => 'avatar') %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t('.delete_html'), user, method: :delete,
                  data: { confirm: t('.confirm_html') } %>
  <% end %>
</li>
